pluginManagement {
    plugins {
        id 'org.springframework.boot' version "${springBootVersion}"
        id 'io.spring.dependency-management' version "1.1.3"
        id 'io.freefair.lombok' version "${lombokPluginVersion}"
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'contentgrid-surveyor'

include 'surveyor-application'
include 'surveyor-dependencies'

include 'surveyor-api'
include 'surveyor-spi'

include 'surveyor-domain'

include 'surveyor-drivers-web'

include 'surveyor-infrastructure-storage-inmemory'
include 'surveyor-infrastructure-storage-data-jdbc'
include 'surveyor-infrastructure-source-events-loki'
include 'surveyor-infrastructure-source-metrics-prometheus'

include 'surveyor-usecases'